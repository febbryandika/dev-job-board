// TODO(phase-3): registration form; role is chosen here but assigned by a
// server-side action that only accepts `candidate | employer`. SPEC §7.
export default function RegisterPage() {
  return <h1 className="text-2xl font-semibold tracking-tight">Create an account</h1>
}
