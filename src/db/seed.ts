// TODO(phase-2): seed 3 demo users (candidate/employer/admin, password
// `demo1234`) and ~20 Japan-flavoured listings covering all four statuses, so
// the moderation queue and the rejected/resubmit path are non-empty on first
// load. SPEC §11.
console.log('Seed script is not implemented yet — nothing to do.')
